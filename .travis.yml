language: python

python:
    - "2.7"

before_install:
    - sudo pip install virtualenv rsync
    - virtualenv $TRAVIS_BUILD_DIR/arteria-delivery/

install:
    - source $TRAVIS_BUILD_DIR/arteria-delivery/bin/activate
    - pip install -e . -r ./requirements/dev

script:
    - nosetests tests/

notifications:
    email: false
